[build]
builder = "NIXPACKS"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["python39"]

[deploy]
startCommand = "streamlit run gui/integrated_interface.py --server.port $PORT --server.address 0.0.0.0"
